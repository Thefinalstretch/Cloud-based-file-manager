// import { createClient } from "@supabase/supabase-js";

// const supasbaseUrl = import.meta.env.VITE_SUPABASE_URL;
// const ServiceRoleKey = import.meta.env.VITE_SUPABASE_SERVICE_ROLE_KEY;

// if (!ServiceRoleKey) {
//   throw new Error("Missing ServiceRoleKey!");
// }

// export const supabaseAdmin = createClient(supasbaseUrl, ServiceRoleKey);
